terraform {
  required_version = ">= 1.6.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}

# Terraform Lite scaffold:
# Add API hosting, frontend hosting, and Key Vault wiring incrementally.
